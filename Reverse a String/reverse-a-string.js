//Function reverses any string argument assigned to it

function reverseString(str) {
    //split the string
    //use the reverse method
    //join the string back together
  return str.split("").reverse().join("");
}

reverseString("hello");
